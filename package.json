{
  "name": "shramigo-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "ShramiGo Monorepo",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
